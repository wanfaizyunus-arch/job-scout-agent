[build]
builder = "NIXPACKS"

[deploy]
# Runs daily at 7:00 AM Malaysia Time (MYT = UTC+8, so 23:00 UTC previous day)
cronSchedule = "0 23 * * *"
startCommand = "python job_agent.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2
